You are resuming a task that was interrupted. Start by running `git status` to check the current state of the working tree. If any conversation history exists, scan recent messages to identify the last completed step. Then continue from where you left off — do not restart the plan from scratch. Any uncommitted changes you see in the working tree are your own prior work; reconcile them with your plan before proceeding.
