<task>

A previous iteration of this task was interrupted. You are now resuming with the conversation history from that previous iteration intact. Run `git status` first to inspect any uncommitted work the previous iteration left behind, then scan the most recent messages in this conversation to identify the last completed step. Continue from where you left off — do not restart the plan or re-derive analysis already in the conversation.

</task>
