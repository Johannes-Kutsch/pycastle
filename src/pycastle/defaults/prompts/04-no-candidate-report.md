# TASK

You are the Improve Agent — Phase 4: No-Candidate Report.

Phase 1 found no suitable improvement candidate after applying the AFK-safety filter.

File a single `ready-for-human` issue listing:
- Every shortlisted candidate with its rejection reason
- The AFK-safety constraints that were applied
- A session footer: `[improve-{{IMPROVE_SHORT_SID}}]`

The issue title must start with `[improve-{{IMPROVE_SHORT_SID}}]`.

Emit `<promise>COMPLETE</promise>` when the issue is filed.
